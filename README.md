# MVC E-commerce Project

Учебный проект системы электронной коммерции (интернет-магазин).
Проект полностью настроен и готов к запуску. Вам нужно лишь скопировать и выполнить команды ниже.

---

## 🚀 Быстрый Старт (Копировать-Вставить)

Выберите вашу операционную систему и выполняйте команды по порядку в терминале (Command Line / Powershell).

### 🍎 macOS
**1. Установка инструментов (если нет):**
Скопируйте и нажмите Enter (установит/обновит Homebrew, Node.js и MongoDB):
```bash
# Установка Homebrew (если нет)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установка Node.js
brew install node

# Установка и запуск MongoDB
brew tap mongodb/brew
brew install mongodb-community
brew services start mongodb-community
```

**2. Установка и запуск проекта:**
```bash
# Клонирование и установка зависимостей
git clone https://github.com/your-username/mvc-ecommerce.git
cd mvc-ecommerce
npm install

# Запуск
node app.js
```

---

### 🪟 Windows
Откройте **PowerShell** (или командную строку) от имени **Администратора**.

**1. Установка программ (через winget):**
```powershell
# Установка Node.js
winget install OpenJS.NodeJS

# Установка MongoDB
winget install MongoDB.Server
```
*После установки перезапустите терминал.*

**2. Запуск Базы Данных:**
Создайте папку для данных и запустите базу (в отдельном окне терминала):
```powershell
md C:\data\db
"C:\Program Files\MongoDB\Server\7.0\bin\mongod.exe" --dbpath="c:\data\db"
```

**3. Запуск проекта (в основном окне):**
```powershell
cd path\to\mvc-ecommerce
npm install
node app.js
```

---

### 🐧 Linux (Ubuntu/Debian)
**1. Установка:**
```bash
# Обновление пакетов
sudo apt update

# Установка Node.js и npm
sudo apt install -y nodejs npm

# Установка MongoDB (пример для Ubuntu)
sudo apt install -y mongodb
sudo systemctl start mongodb
sudo systemctl enable mongodb
```

**2. Запуск:**
```bash
git clone https://github.com/your-username/mvc-ecommerce.git
cd mvc-ecommerce
npm install
node app.js
```

---

## ✅ Как проверить, что все работает?
1. В терминале должно быть написано: `Server started on http://localhost:3000`
2. Откройте в браузере ссылку: [http://localhost:3000/products](http://localhost:3000/products)
3. Вы должны увидеть список товаров.

## 📚 О Проекте
Проект демонстрирует архитектуру **MVC** и паттерны проектирования:
- **Singleton**: База данных.
- **Factory**: Пользователи.
- **Facade**: Покупки.
- **Observer**: Логи.
- **Worker Threads**: Отчеты.

Подробное описание архитектуры находится в файле [PROJECT_REPORT.md](PROJECT_REPORT.md).
