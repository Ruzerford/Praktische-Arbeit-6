# MVC E-commerce Project

Учебный проект системы электронной коммерции, разработанный в рамках практической работы. Проект демонстрирует применение архитектуры **MVC**, а также внедрение основных паттернов проектирования (**GoF**).

## 🚀 Особенности
- **Архитектура**: Строгое разделение на Model, View, Controller.
- **Паттерны проектирования**:
  - **Singleton**: Управление подключением к БД.
  - **Factory**: Создание пользователей.
  - **Adapter**: Интеграция с legacy платежной системой.
  - **Facade**: Упрощение процесса покупки.
  - **Observer**: Логирование событий.
  - **Strategy**: Сортировка товаров.
  - **Producer-Consumer**: Фоновая генерация отчетов (Worker Threads).
- **Стек технологий**: Node.js, Express, MongoDB (Mongoose), EJS.
- **Дизайн**: Apple-style минимализм с использованием CSS Glassmorphism.

## 🛠 Предварительные требования
Для работы проекта необходимы установленные:
- **Node.js** (v14+)
- **MongoDB**

---

## 💻 Установка и Запуск

### 1. Подготовка проекта (Все ОС)
Клонируйте репозиторий и установите зависимости:
```bash
git clone https://github.com/your-username/mvc-ecommerce.git
cd mvc-ecommerce
npm install
```

### 2. Запуск Базы Данных (MongoDB)

#### 🍎 macOS
Если у вас установлен Homebrew:
```bash
brew services start mongodb-community
```
*Альтернативно:* Запустите `mongod` в отдельном терминале.

#### 🐧 Linux (Ubuntu/Debian)
Используйте systemd:
```bash
sudo systemctl start mongod
```
Проверьте статус: `sudo systemctl status mongod`

#### 🪟 Windows
1. Откройте **Службы** (Win + R -> `services.msc`).
2. Найдите **MongoDB Server** и нажмите **Запустить**.
*Альтернативно:* В командной строке (от имени администратора):
```cmd
net start MongoDB
```

### 3. Запуск Сервера
После успешного запуска базы данных:
```bash
node app.js
```
Вы увидите сообщение: `Server started on http://localhost:3000`

### 4. Использование
Перейдите в браузере по адресу: [http://localhost:3000/products](http://localhost:3000/products)

---

## 📂 Структура Проекта
- `src/models`: Схемы данных (Mongoose).
- `src/views`: Шаблоны страниц (EJS).
- `src/controllers`: Обработчики запросов.
- `src/utils`: Утилиты (подключение к БД, сидеры).
- `src/workers`: Фоновые задачи (Worker Threads).

## 📝 Документация
Подробный отчет о выполнении задания доступен в файле [PROJECT_REPORT.md](PROJECT_REPORT.md).
